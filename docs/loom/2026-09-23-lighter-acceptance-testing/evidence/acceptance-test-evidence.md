# Make acceptance testing lighter without losing what only it catches — acceptance test evidence

Tried on 2026-09-23, in a clean copy of the project at e43b6b11.

## Setup check (every run)
- How I tried it: `git worktree add --detach <scratchpad>/at-wt HEAD` (HEAD e43b6b11, `git status --short` empty); followed the root README "Install" section. Installing into the user's real Claude Code would replace their installed copy, so I loaded the plugin from the clean copy instead: `claude plugin validate ./loom-code`, then `python3 loom-code/scripts/loom_checker.py --list-rules`, `reviewer-count 2026-09-23-lighter-acceptance-testing`, `selection show 2026-09-23-lighter-acceptance-testing`.
- What came back: `✔ Validation passed` (exit 0); the checker listed its rules; `reviewer-count` printed `2` (exit 0); `selection show` printed the selection JSON (exit 0).
- Evidence: captured output above. Worktree removed afterwards (`git worktree remove --force`); `git worktree list` no longer shows it.

## 1. The acceptance tester does not run the full package suite; a criterion the suite settles cites the finalize-review result instead. A setup check from a fresh clone still runs every time.
- How I tried it: dogfood — ran this acceptance test under the branch's contract (`loom-code/agents/acceptance-tester.md` steps 1, 2, 6). Did not run the package suite; ran only the tests covering this line: `python3 -m pytest -q loom-code/scripts/test_acceptance_test_report_shape.py "loom-code/scripts/test_write_plan_station_text.py::test_current_release_metadata_is_synchronized"`. Read `acceptance-tester.md:39-51` (step 6) and `closing-review/SKILL.md:218-221`.
- What came back: `24 passed in 0.22s`. Step 6 says "Never run the full package suite", and a suite-settled row "cites the suite command and says `finalize-review` executes it and refuses the attestation when it fails". Step 2 says the setup check "happens on every run, re-runs included". Following it was practical: the full suite was never needed to reach a verdict here.
- Evidence: the pytest output; `test_suite_criterion_cites_finalize_review_command`, `test_no_full_suite_run_instruction`. Suite command for the full package: the repository's declared test command, which `finalize-review` executes (`command_handlers/finalize.py:132-136`) and refuses the attestation on failure; the orchestrator reports it exited 0 at e43b6b11.
- Gap: the intent says "cites the finalize-review result"; the contract (and plan Risk 1) has the row cite the check instead, because the report is committed before finalize-review runs. Also the contract allows a worktree where the intent says "fresh clone".
- Verdict basis: `works`.

## 2. The report the user reads holds one row per criterion (verdict and one plain sentence), the decisions made on the user's behalf, and the open questions; the evidence behind each row lives in a separate file under the change's evidence directory.
- How I tried it: wrote this change's own report from `references/acceptance-test-report.md` (template lines 22-68, evidence shape lines 72-95), with the evidence in this file. Committed a plain `.md` file at `docs/loom/2026-09-23-lighter-acceptance-testing/evidence/acceptance-test-evidence.md` in the scratch worktree and ran `check_adversarial_proportionate` + `committed_probe_programs` (scratchpad `prop.py`). Negative control: same path with `#!/bin/sh` as first line. Read `land.py:_acceptance_line` (reads only the report path) and `reviewers.py:59-72` (paths under `docs/loom/<change-id>/` are low risk).
- What came back: plain `.md`: `stored programs: []`, `findings: []`. `#!` control: `adversarial.proportionate` refuses it as "a program in this change's store outside .../evidence/probes/" — which matches the template's "never starting with `#!`". The report came out at 48 lines (including three sections the template lacks, see below) versus about 100 lines median before.
- Evidence: captured output above; `test_template_has_one_row_per_criterion_and_evidence_file_path`, `test_evidence_file_shape_is_given_and_is_plain_markdown`, `test_template_row_carries_no_how_or_evidence_cell` passed in the run under line 1.
- Not tried: a full `finalize-review` and `land` run — finalize-review needs reviewer verdicts not yet produced and executes the package suite; land merges a PR. Their behaviour on the evidence file is from the code read and the proportionate check above.
- Friction found while writing (see report "I decided for you"): (a) the contract says the report's section list is the manifest charter's `must` column (`contract/manifest.yaml:211-216`: adds "Review summary" and "Questions I asked you"), which the template lacks; (b) the contract requires a list of whether the English rule held per artifact, and the template has no section for it; (c) step 6 says a suite-settled row "cites the suite command", while the template forbids commands in the report.
- Verdict basis: `works`.

## 3. A re-run after a fix tests only the criteria the fix could affect, marks every other verdict as carried over from the earlier run, and gives a one-line reason for each carried-over verdict. A criterion that is re-tested is re-tested in full — every surface the Acceptance line names — never only the part the fix touched.
- How I tried it: this is a first run, so no real re-run happened. Read step 7 (`acceptance-tester.md:52-57`), the dispatch inputs (`:17-19`), `closing-review/SKILL.md:222-226`, template Re-run column and the evidence file's re-run section. Read as a cold tester: which rows to re-test (those the fix could affect, judged against the fix diff), how far (every surface the line names), what to write for the rest (`carried over — <reason>`), and what to do when unsure (re-test in full). Ran the covering tests in the run under line 1.
- What came back: the rule reads unambiguously for criteria rows. Two small gaps: it speaks of "criteria" while the template also puts spec UI-flow rows in the table (a product change's UI-flow row on a re-run is not named); and "the setup check" is not a row, but step 2 covers it on every run.
- Evidence: `test_rerun_retests_every_named_surface`, `test_partial_surface_retest_forbidden`, `test_rerun_dispatch_passes_earlier_report_evidence_and_fix_range`, `test_row_has_carried_over_marker_with_reason`, `test_retested_row_has_no_carry_reason` passed.
- Verdict basis: `partly` — the rules and structure are in place and tested, but no real re-run after a fix has been exercised.

## 4. These rules are carried by the acceptance tester's contract and the report template's own structure, and the checker's rule list does not grow.
- How I tried it: `python3 loom-code/scripts/loom_checker.py --list-rules | grep -c .` in the clean copy at e43b6b11 and in a second clean worktree at `main` (6fe96189); `git diff --stat main...HEAD -- loom-code/scripts/loom_checker loom-code/contract`; `git diff main...HEAD | grep -n 'gate:'`.
- What came back: 26 rules on both; no diff under the checker or the contract; the only `gate:` hits are inside the new test asserting no new gate marker (diff lines 605-608).
- Evidence: captured counts; `test_rules_live_in_contract_and_template`, `test_no_new_gate_marker` passed.
- Verdict basis: `works`.

## Re-run on 2026-09-23, at f834349e
Re-run inputs: the earlier report and this file (sections above are the first run at e43b6b11, kept as recorded), fix range `e5282665..f834349e` (cf57f1a6, f834349e). The fix diff touches `agents/acceptance-tester.md` (section-list sentence :15-16, step 6 :43-50, English-list paragraph :74-81), `closing-review/SKILL.md` §3 :218-221, `contract/manifest.yaml` :210-215 (charter `must`: drops "Review summary" and "Questions I asked you", adds "open questions"), `test_acceptance_test_report_shape.py` (phrase rename), CHANGELOG. Step 7 (`acceptance-tester.md:58-63`) and the template are unchanged.

Setup check (every run): `git worktree add --detach <scratchpad>/at-rerun-wt HEAD` → detached f834349e, `git status --short` empty. README "Install" section installs from the marketplace, which would replace the user's installed copy, so loaded from the clean copy: `claude plugin validate ./loom-code` → `✔ Validation passed` (exit 0); `loom_checker.py --list-rules | grep -c .` → 26; `contract --require 2.3` → `contract 2.3.1 satisfies requires-contract >=2.3` (exit 0; a first attempt with `--require 3.11` used the plugin version by mistake and was refused `BLOCK contract.requires` — tester error, not a defect); `reviewer-count 2026-09-23-lighter-acceptance-testing` → `2` (exit 0); `selection show …` → JSON, `run` includes `acceptance-test` and `package-tests`, `skip: []` (exit 0). Worktree removed with `git worktree remove --force`; `git worktree list | grep -c at-rerun` → 0.

Covering tests only (no package suite): `python3 -m pytest -q loom-code/scripts/test_acceptance_test_report_shape.py loom-code/scripts/test_contract_charter.py "loom-code/scripts/test_write_plan_station_text.py::test_current_release_metadata_is_synchronized"` → `35 passed in 0.83s`. Full-suite check the report row names in plain words: the repository's declared test command, executed by `finalize-review` (`command_handlers/finalize.py`), which refuses the attestation on failure; not run here.

- 1: re-tested — the fix rewrote step 6 and SKILL §3 (the suite-row wording). Surfaces: (a) no full package suite run — none was run in this re-run, only the 35 covering tests above; (b) suite-settled criterion cites finalize-review — step 6 (`acceptance-tester.md:46-50`) now has the row "name the suite check in plain words" with the fixed phrase "the automated test suite, which runs before the change is accepted and blocks it on failure", command to this file; SKILL §3 :218-221 says the same; the template forbids commands in the report, so the first run's contradiction (c) is gone; `test_suite_criterion_cites_finalize_review_command` passed; (c) setup check from a fresh copy on every run — step 2 (`:31-32`) unchanged and done above. Residual gap unchanged from the first run: the intent says "cites the finalize-review result", the contract cites the check (result not available before the report is committed); a worktree stands in for a fresh clone. Verdict: works.
- 2: re-tested — the fix changed where the report's section list comes from (`:15-16`, now the template), the manifest charter `must`, and moved the English-rule list to this file (`:74-81`, "That list stays out of the report" :80). Surfaces: (a) one row per criterion with verdict and one plain sentence — the rewritten report has four rows, one sentence each; (b) decisions on the user's behalf and (c) open questions — present as template sections; template sections = {rows, existing-data paragraph, I decided for you, open questions} and manifest `must` = {one line per Acceptance, What this did to existing data, I decided for you, open questions}: they now match, so the three extra sections the first run added (review summary, questions asked, English rule) were removed; `test_charter_command_on_real_manifest_exits_zero` passed; (d) evidence in a separate file under the evidence directory — `prop.py` on the committed tree at f834349e: `stored programs: []`, `findings: []`; negative control (same file with `#!/bin/sh` first line, chmod +x, committed in the scratch worktree as 137c3cbd): `adversarial.proportionate` refuses it as "a program in this change's store outside …/evidence/probes/". Small friction: the template's evidence-file shape has no slot for the English-rule list the contract now puts here; placed as the last section. Verdict: works.
- 3: re-tested — step 7 and the template's Re-run column did not change, but the first-run verdict (partly) rested on no real re-run having happened; step 7's "any doubt means re-testing that criterion in full" applies. Surfaces, exercised on this real re-run: (a) test only what the fix could affect — did the triage against the diff above; every line had a concrete reason (1: step 6/§3; 2: section list, charter, English list; 4: `contract/manifest.yaml` is read by the checker via `load_manifest`, and `contract.charter-complete` checks the `must` column, `rule_checks/charter.py:75`); (b) carried-over rows with one-line reasons — zero rows qualified, so this path was not exercised; (c) re-test in full over every named surface — done for all four lines. Inputs the contract promised (earlier report, evidence, fix range) were sufficient. Verdict: partly (the carry-over half was not exercised by a real run).
- 4: re-tested — the fix touched `loom-code/contract/`, which the first run reported as unchanged. Rule count 26 at HEAD f834349e and 26 at the merge base with main (6fe96189, second temporary worktree, removed); `git diff --stat 6fe96189..HEAD -- loom-code/scripts/loom_checker loom-code/scripts/loom_checker.py loom-code/contract` → only `contract/manifest.yaml | 3 +--`; `git diff 6fe96189..HEAD | grep -c '^+.*<!-- gate:'` → 3, all inside the test asserting no new gate marker. The manifest change edits one charter's `must` list, not a rule. `test_rules_live_in_contract_and_template`, `test_no_new_gate_marker` passed in the 35. Verdict: works.

## English rule and naming, per artifact (as of f834349e)
- Plan: English, apart from the two user answers quoted verbatim under "Questions asked" (`plan.md:42-43`).
- Spec: none (`needs-design: no`); EARS `REQ-<n>` rule not applicable.
- Reviewer findings: none handed to me; Conventional Comments label not checkable yet.
- Evidence: this file, English.
- Test docstrings: English (`test_acceptance_test_report_shape.py` checked for CJK: none).
- Test names: the 10 graduated probes follow `test_<unit>_<state>_<expected>` (e.g. `test_suitecheck_pytestoverwholepackage_turnsred`); the 13 plan tests are sentence-style (e.g. `test_template_has_one_row_per_criterion_and_evidence_file_path`) without a clear state/expected split. The fix renamed no test.
- Commit messages: 12 commits `main..HEAD`, English Conventional Commits, no CJK (`git log --format=%B main..HEAD` byte scan: 0 hits).

## Re-run on 2026-09-23, at 19025419
Re-run inputs (closing-review §3 as of 19025419): skip state — `package-tests` not skipped, `finalize-review` will run; the earlier report and this file (sections above kept as recorded); fix range `f950eaf9..19025419` (one commit, 19025419). The fix diff touches `agents/acceptance-tester.md` (:22-24 station passes skip state; step 6 :51-52 a failing covering test makes the row `fails`, :57-60 skip wording now keyed to what the station says; step 7 :61-64 re-test scope widened to "rows … Acceptance lines and, for a product change, UI flows"; the per-artifact English-rule list dropped from "What you write" :78-81; the vocabulary trap now allows the one evidence pointer line :104-106), `closing-review/SKILL.md` §3 :218-221 (suite-row and re-run prose replaced by "tell the tester whether `package-tests` or `finalize-review` is skipped … steps 6-7 govern"), `test_acceptance_test_report_shape.py` (7 probe tests folded into matcher assertions, `STATION_GATES` set assertion and the §3 partial-trap assertion removed), `plan.md` risk 4, CHANGELOG. Not touched: the report template, `contract/`, the checker. The section "English rule and naming, per artifact" above is no longer required by the contract (dropped at :78-81); left as recorded.

Setup check (every run): `git worktree add --detach <scratchpad>/at-rerun2-wt HEAD` → detached 19025419, `git status --short | wc -l` → 0. README "Install" installs from the marketplace, which would replace the user's installed copy, so loaded from the clean copy: `claude plugin validate ./loom-code` → `✔ Validation passed` (exit 0); `loom_checker.py --list-rules | grep -c .` → 26; `contract --require 2.3` → `contract 2.3.1 satisfies requires-contract >=2.3` (exit 0); `reviewer-count 2026-09-23-lighter-acceptance-testing` → `2` (exit 0); `selection show …` → `run` includes `acceptance-test` and `package-tests`, `skip: []` (exit 0). Worktree removed with `git worktree remove --force`; `git worktree list | grep -c "at-rerun2\|at-base"` → 0.

Covering tests only (no package suite): `python3 -m pytest -q loom-code/scripts/test_acceptance_test_report_shape.py loom-code/scripts/test_contract_charter.py "loom-code/scripts/test_write_plan_station_text.py::test_current_release_metadata_is_synchronized"` → `28 passed in 0.82s` (35 at f834349e; the shape file went from 23 to 16 tests because the fix folded 7 probe tests into `test_suitecheck_negatedinstruction_exempt`). Full-suite check the report row names in plain words: the repository's declared test command, executed by `finalize-review`, which refuses the attestation on failure; not run here, per the station's skip state.

Triage against the fix diff: every row depends on text the fix changed, so none is carried over.
- 1: re-tested — the fix rewrote step 6 (skip state from the station, `fails` verdict) and replaced the §3 suite-row prose. Surfaces: (a) no full package suite run — none run, only the 28 covering tests; (b) suite-settled criterion cites finalize-review — step 6 with the station's input "not skipped, will run" makes the row name the check in the fixed plain-words phrase, command in this file; §3 now only passes the skip state and defers to steps 6-7; `test_suite_criterion_cites_finalize_review_command` passed; the `fails` branch was not exercised (no covering test failed); (c) setup check from a fresh copy every run — step 2 unchanged, done above. Residual gap unchanged: the intent says "cites the finalize-review result", the contract cites the check (no result exists before the report is committed); a worktree stands in for a fresh clone. Verdict: works.
- 2: re-tested — the fix removed the English-rule list from what the tester writes (it lived in this file) and changed the report vocabulary rule (pointer-line exception). Surfaces: (a) one row per criterion, verdict and one plain sentence — the rewritten report has four rows, one sentence each; (b) decisions on the user's behalf and (c) open questions — template sections present; manifest `must` for `acceptance-test-report` = {one line per Acceptance, What this did to existing data, I decided for you, open questions}, matching the template (`test_charter_command_on_real_manifest_exits_zero` passed); the pointer line is the report's only path; loom terms removed from the decision bullets (the verbatim Acceptance line 1 in the table still contains "finalize-review", as the template requires the line verbatim); (d) evidence in a separate file under the evidence directory — scratchpad `prop.py` on the clean tree at 19025419: `stored programs: []`, `findings: []`; negative control (same path rewritten to start with `#!/bin/sh`, chmod +x, committed in the scratch worktree as 5ecc5265): `adversarial.proportionate` refuses it as "a program in this change's store outside …/evidence/probes/". Verdict: works.
- 3: re-tested — the fix rewrote step 7 (rows, UI flows) and removed the §3 restatement of the partial re-test trap and its test assertion. Surfaces, exercised on this real re-run: (a) test only what the fix could affect — triage done above against the diff; (b) carried-over rows with one-line reasons — zero rows qualified again, so this path is still not exercised by a real run; the tester contract still names the partial re-test trap with a negation (`test_partial_surface_retest_forbidden` passed); (c) re-test in full over every named surface — done for all four. UI-flow rows: step 7 now names them; this change has no spec (`needs-design: no`), so none apply. Verdict: partly.
- 4: re-tested — the fix moved rules from §3 prose into the tester contract, which is the line's subject, and removed the station gate-set assertion from `test_no_new_gate_marker`. Surfaces: (a) rules carried by the tester contract — the skip-state input, `fails` verdict and UI-flow scope now live in `acceptance-tester.md`; §3 carries only the dispatch inputs; (b) template structure — template unchanged by the fix; (c) checker rule list does not grow — 26 at 19025419 and 26 at the merge base 6fe96189 (second temporary worktree `at-base-wt`, removed); `git diff --stat 6fe96189..19025419 -- loom-code/scripts/loom_checker loom-code/scripts/loom_checker.py loom-code/contract` → only `contract/manifest.yaml | 3 +--` (from cf57f1a6, a `must` list edit, not a rule); `git diff f950eaf9..19025419 -- loom-code/skills/closing-review/SKILL.md | grep -c 'gate:'` → 0; the station's gate markers at 19025419 are exactly `review.absence-recovery`, `review.atomic-claude-dispatch`, `review.bounded-episode`, `review.probe-graduation` — the set the removed assertion pinned. `git diff 6fe96189..19025419 | grep -c '^+.*<!-- gate:'` → 3: one inside this file's earlier re-run text, two inside the test asserting no new gate marker. `test_rules_live_in_contract_and_template`, `test_no_new_gate_marker` passed. Verdict: works.

Earlier open questions, re-judged:
- No slot for the English-rule list in the evidence shape — closed: the fix drops the tester's per-artifact English audit (`acceptance-tester.md:78-81`); the reviewer contract still reads every internal artifact for English (`agents/reviewer.md:84-88`).
- UI-flow rows on a re-run — closed: step 7 now covers "Acceptance lines and, for a product change, UI flows" (`acceptance-tester.md:61-64`).
