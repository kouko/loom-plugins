# A fix round starts from every blocking finding, grouped by defect class — acceptance test evidence

Tried on 2026-09-24, in a clean copy of the project at 3e03f4e7 (control: branch base 4a36bb60).

## Setup
- How I tried it: `git worktree add --detach $S/wt-head 3e03f4e7` and `git worktree add --detach $S/wt-base 4a36bb60` (`$S` = this session's scratchpad). The README install path (`claude plugin marketplace add https://github.com/kouko/loom-plugins.git` then `claude plugin install loom-code@loom`, README.md:178-179) installs what is published on main, so the unpublished branch was loaded the way Claude Code loads a local plugin: `claude -p ... --plugin-dir $S/wt-head/loom-code` (control: `--plugin-dir $S/wt-base/loom-code`). No `--disable-slash-commands`.
- What came back: `plugin.json` versions 3.13.0 (branch) and 3.12.0 (base); README table shows loom-code 3.13.0. Every trial session's `init` event listed `loom-code` at the matching worktree path, `source: loom-code@inline`, with version 3.13.0 (branch) or 3.12.0 (control). Every session's first tool call was `Skill {"skill": "loom-code:closing-review"}` and its tool result began `Base directory for this skill: $S/wt-head/loom-code/skills/closing-review` (control: `wt-base`). The new rule's first sentence ("Before any fix begins, the main agent collects") is present in the loaded skill text in all 5 branch sessions and absent in all 5 control sessions.
- Evidence: transcripts `$S/hr/{new1,new2,new3,ctl1,ctl2,ctl3,b-new1,b-new2,b-ctl1,b-ctl2}.jsonl`; summarizers `$S/hr/summarize.py`, `$S/hr/summarize_b.py`; full answers `$S/hr/full.txt`, `$S/hr/full_b.txt`. Claude Code 2.1.281, model `claude-opus-5-5[1m]`, total trial cost about USD 4.58.

### The seeded case
- Throwaway repo `$S/hr/seed`: `main` has `app/legacy.py` (pre-existing `os.path.join(LEGACY_DIR, name)` — the distractor outside the change); branch `feat/export-and-attachments` adds an intent with three Acceptance lines (export inside the user's folder; attachments only from the note's folder; 20 per page, every note on one page), `app/export.py` (unchecked `os.path.join(folder, filename)`, line 10), `app/attachments.py` (unchecked `folder + "/" + name`, line 8), `app/listing.py` (`total // PAGE_SIZE`, line 5), and one test. Each session got its own clone (`$S/hr/trial-*`).
- Findings on one digest: reviewer R1 `NEEDS_REVISION` — path finding at `app/export.py:10` (important) + a nit; reviewer R2 `NEEDS_REVISION` — page-count finding at `app/listing.py:5` (important); acceptance tester — path finding at `app/attachments.py:8` (important). Two findings share a defect class (one reviewer, one tester), one is unrelated, and both reviewers carry the same verdict label, so grouping by label would be wrong.
- Case A (`$S/hr/prompt.txt`): all three sources given together with ids F1-F4. Case B (`$S/hr/prompt2.txt`): the acceptance tester's return arrives first as its YAML block ("earlier today", id `AT-1`), the reviewers' after (ids `R1-1`, `R1-2`, `R2-1`).
- Command per session (`$S/hr/trials.sh`, `$S/hr/trials2.sh`): `claude -p "$P" --plugin-dir <loom-code> --output-format stream-json --verbose --allowedTools "Read,Grep,Glob,Bash(git *),Skill" --disallowedTools "Edit,Write,Agent,NotebookEdit"`. The prompt says to prepare what goes to Build without fixing, editing, dispatching or running the checker, to say what happens to the reviewer verdicts, and to end with a `HANDOFFS:` summary block. It never says to group by class or to include the tester's findings.
- Distractors: after all 10 sessions, `git status --short` is empty in every trial clone and `app/legacy.py` is unchanged. Every session that mentioned `app/legacy.py` put it out of bounds (intent's Out of scope).
- Side note: while seeding, a first `git init` landed in an older scratch repo from an earlier session (`$S/seed`), adding one commit to its `feat/report-queries` branch and a new branch; a guard blocked the ref cleanup, so it was left as is. That repo is not used by any trial here and is outside this project.

## 1. Before a fix round starts, every fatal or important finding from the reviewers and from independent acceptance testing on the current version is collected into one list, and no fix begins from a subset of it.
- How I tried it: `python3 -m pytest -q loom-code/scripts/test_fix_handoff_text.py loom-code/scripts/test_fix_scope_text.py` in `wt-head`; read each trial's collected list and whether all hand-offs go to Build in one round.
- What came back: `8 passed` (3 in `test_fix_handoff_text.py`: `test_every_rule_sentence_is_pinned_exactly_and_record_kept_verbatim`, `test_weakened_or_inserted_paragraph_fails_the_pins`, `test_no_gate_marker_dispatch_words_or_rule_growth`; 5 in `test_fix_scope_text.py`). Trials: all 5 branch sessions listed the three important findings — including the acceptance tester's — and left out the nit; all 5 said every hand-off goes in the same fix round, before the reviewers resume (e.g. new1: "two hand-offs to Build, both in the same fix round. The nit F4 is left out of the fix list."; new1 table "Collected fix list (digest `c0ffee12`)" with F1/R1, F3/Acceptance tester, F2/R2). Control: all 5 also included the tester's finding and sent everything in one round.
- Evidence: named tests; rule text `loom-code/skills/closing-review/SKILL.md:292-301`; transcripts `new*.jsonl`, `b-new*.jsonl`. Full suite (`python3 -m pytest` over `loom-code/scripts`, run by `finalize-review`) not run here, per the tester contract.

## 2. Findings in that list that share a defect class, named in words from the findings themselves and not from the recorded verdict label, reach Build as one hand-off that names all of their instances.
- How I tried it: same test run (exact-sentence pins and inserted-word negatives); read each trial's `HANDOFFS:` block and hand-off text.
- What came back: branch 5/5 produced 2 hand-offs — {path finding at export, path finding at attachments} and {page count} — each class named in the findings' words ("Caller-supplied file name escapes its base folder", "A caller-supplied name joined to a base folder with no check that the result stays inside it", "Page count drops the partial last page"); none used `NEEDS_REVISION` as a class, and new1/b-new1/b-new2 said explicitly that the label names no class / defines no fix. Control 4/5 did the same; ctl3 sent one hand-off "Round 1 fix batch" covering F1, F2, F3 (it noticed F1 and F3 are the same defect but did not split the unrelated page-count finding out).
- Evidence: summaries in `$S/hr/full.txt`, `$S/hr/full_b.txt`; named tests above.

## 3. The station text states that the per-verdict failure record kept for the pull request is disclosure only and is not the input a fix is scoped from.
- How I tried it: read `loom-code/skills/closing-review/SKILL.md:300-301` in `wt-head`; `test_every_rule_sentence_is_pinned_exactly_and_record_kept_verbatim` (pins the sentence and keeps the record-failure sentence verbatim); `test_simplified_station_text.py` (54 passed); checked each trial's answer for how it treated the record.
- What came back: SKILL.md:300-301 reads "The per-verdict failure record below is disclosure for the pull request only; each fix is scoped from this list." The record-failure command sentence right after it is unchanged from base. Branch 5/5 trials said the record is disclosure only / does not decide what is fixed (e.g. b-new2: "That record is only disclosure for the pull request; it doesn't decide what gets fixed."); control 0/5 said so (they record the verdicts without saying what role the record plays).
- Evidence: file:line above; named tests; transcripts.

## 4. Given a seeded case with two reviewer findings and one acceptance-testing finding, two of which share a defect class, an agent following the station text produces one hand-off for the shared class and a separate one for the other, shown by a trial run.
- How I tried it: the seeded case above, two prompt variants, branch arm 3 + 2 sessions, control arm 3 + 2 sessions.
- What came back: branch 5/5: `HANDOFFS: 2`, shared class = {reviewer's export finding, tester's attachment finding}, separate = {page count}. Control (3.12.0) 4/5 the same, 1/5 a single merged hand-off. Neither arm edited any file.
- Evidence: `new1`–`new3`, `b-new1`–`b-new2` (branch) and `ctl1`–`ctl3`, `b-ctl1`–`b-ctl2` (control) transcripts and summary blocks.

## 5. The rule is carried by station prose; the checker's rule list does not grow and no new step, reviewer or dispatch is added.
- How I tried it: `python3 loom-code/scripts/loom_checker.py --list-rules | wc -l` in both worktrees; `python3 loom-code/scripts/check_mechanisms.py --baseline 4a36bb60` in `wt-head`; `git diff 4a36bb60..HEAD -- loom-code/skills loom-code/agents | grep '^+' | grep -iE "gate|dispatch|subagent|step"`; `test_no_gate_marker_dispatch_words_or_rule_growth`; `python3 -m pytest -q loom-code/scripts/test_write_plan_station_text.py -k synchronized`.
- What came back: 26 rules on base and branch; `prose-gate 20 20`, `net mechanism count (excl. host-hygiene): 140`, `baseline net count: 140`, `all clear`; the grep found no added line (exit 1); `1 passed, 48 deselected`. Added station text: one unmarked paragraph in closing-review (SKILL.md:292-301).
- Evidence: commands and outputs above.

## Re-run on 2026-09-24, at b5749b78
Fix range 34987edc..b5749b78 changes only `loom-code/scripts/test_fix_handoff_text.py` (`all_pinned` also requires the paragraph to equal the pinned sentences joined; two insertion cases added to `WEAKENED`). Clean copy: `git worktree add --detach $S/wt-r2 b5749b78` (HEAD `b5749b78e60e0452252f92ebee436ab681a8a653`, `git status --short` empty, `plugin.json` 3.13.0). `git diff --quiet 3e03f4e7 b5749b78 -- loom-code/skills loom-code/agents` exit 0 (station text identical to the first run). Setup re-checked: both trial sessions' `init` listed `loom-code` at `$S/wt-r2/loom-code`, `source: loom-code@inline`, version 3.13.0; first tool call `Skill {"skill": "loom-code:closing-review"}`, base directory `$S/wt-r2/loom-code/skills/closing-review`, new rule sentence present in the loaded text. Trials: `$S/hr/trials3.sh` (same command and prompts as the first run, `--plugin-dir $S/wt-r2/loom-code`), transcripts `$S/hr/r2-a.jsonl` (case A), `$S/hr/r2-b.jsonl` (case B), cost USD 0.466 + 0.464. After both, `git status --short` empty in `trial-r2-a`/`trial-r2-b` and `app/legacy.py` unchanged vs `main`; both put `app/legacy.py` out of scope.
- 1: re-tested — `python3 -m pytest -q loom-code/scripts/test_fix_handoff_text.py loom-code/scripts/test_fix_scope_text.py loom-code/scripts/test_simplified_station_text.py` → `62 passed` (the three `test_fix_handoff_text.py` tests each PASSED, `-v`); SKILL.md:292-301 read in `wt-r2`, unchanged. r2-a list: F1 (R1), F3 (acceptance tester), F2 (R2), nit F4 left out; "two separate fix requests in the same fix round". r2-b: AT-1, R1-1, R2-1 listed, nit R1-2 left out; "two fix hand-offs, both in the same fix round".
- 2: re-tested — same test run. r2-a: `HANDOFF 1: F1, F3 — Caller-supplied name escapes its base folder (path traversal)`, `HANDOFF 2: F2 — Page count rounds down, dropping the last partial page`; "The verdict label only goes on the pull request's failure record. It doesn't decide how Build groups the fixes". r2-b: `HANDOFF 1: AT-1, R1-1 — Unchecked user file name escapes its base folder`, `HANDOFF 2: R2-1 — ...`. Class names come from the findings' words; neither uses `NEEDS_REVISION` as a class.
- 3: re-tested — SKILL.md:300-301 still reads "The per-verdict failure record below is disclosure for the pull request only; each fix is scoped from this list."; `test_every_rule_sentence_is_pinned_exactly_and_record_kept_verbatim` PASSED; `test_simplified_station_text.py` in the 62 passed. r2-a: "The verdict label only goes on the pull request's failure record. It doesn't decide how Build groups the fixes; the findings list does that." r2-b: "This record is only disclosed in the pull request; it doesn't decide what gets fixed."
- 4: carried over — the fix touches only a test file; the station text the agent loads is byte-identical to the first run (skills/agents diff empty), and this line is judged only on the agent's hand-offs. (Incidentally r2-a and r2-b both produced `HANDOFFS: 2` with the shared class together, matching the first run.)
- 5: re-tested — in `wt-r2`: `loom_checker.py --list-rules | wc -l` → 26; `check_mechanisms.py --baseline 4a36bb60` → `prose-gate 20 20`, `net mechanism count (excl. host-hygiene): 140`, `baseline net count: 140`, `all clear`; `git diff 4a36bb60..HEAD -- loom-code/skills loom-code/agents | grep '^+' | grep -iE "gate|dispatch|subagent|step"` exit 1; `test_no_gate_marker_dispatch_words_or_rule_growth` PASSED; `pytest -q loom-code/scripts/test_write_plan_station_text.py -k synchronized` → `1 passed, 48 deselected`. Full suite (`python3 -m pytest` over `loom-code/scripts`, run by `finalize-review`) not run here, per the tester contract.
