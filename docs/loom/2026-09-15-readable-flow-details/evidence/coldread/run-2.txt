# skill-blob: 31476fb1e47ef0086a7f3864d89098914e19cf64
# model: sonnet
# run: 2

=== STEP4 ITEM 5 ===
5. 以下是訪談中你提過、會一併交給下一站（write-plan）寫進 spec 的細節：

| 項目 | 內容 |
|---|---|
| 匯出格式 | CSV（預設） |
| 覆蓋保護 | 目標檔案已存在時，先詢問是否要覆蓋，不會直接蓋掉 |
| 完成訊息 | 匯出完成後顯示一行「已匯出 N 筆」 |
| 檔名 | 固定為 `todo-export`（不含日期） |
| 執行情境 | 你通常會用 cron 排程執行這個指令 |

=== STEP5 CARRIED ===
匯出格式：CSV（預設）
目標檔案已存在時，先詢問是否要覆蓋，不會直接蓋掉
匯出完成後顯示一行「已匯出 N 筆」
檔名固定為 todo-export（不含日期）
使用情境：你通常在 cron 排程中執行這個指令
