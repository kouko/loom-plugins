# Blind-run judgments of the round-2 live trials (Acceptance 6, 7, 9)
# Judged by the blind runner against each Acceptance line's exact words.
# Trial records: a6-rewrite-1..3.txt, a7-how-question.txt, a7-yes-no.txt,
# a9-progress-natural.txt, a9-progress-skill.txt, a9-incident-natural.txt,
# a9-incident-skill.txt. Card delivery per session: trial-card-delivery.txt.
# Setup: fresh `claude -p --model sonnet` per trial, loom-workflow 5.3.0 loaded
# from a clean worktree at dda0014f with --plugin-dir; the real hook injected
# the coexist card (the user's config has ascii-graph-toolkit enabled). Round 1
# (round1-setup-invalid/) is not evidence: skills were disabled by a flag.
# Limit: kouko's earlier reply is pasted into the user message, because a
# print-mode session cannot be seeded with a prior assistant turn.

== Acceptance 6: rewrites of three past hard-to-read replies ==

a6-rewrite-1 (input: Reviewer B round-3 verdict, confirm 3 behaviour sentences)
  skill invoked: no. plain-language.md read: no (Glob in the empty working
  directory found nothing).
  first sentence states the conclusion: yes, but preceded by a bold label
    "**結論：**" (the guide's step 1 says no heading before it).
  metaphor or analogy: FAIL. "蓋上新的驗證章" and "我才能蓋章" describe
    recomputing a hash as stamping a seal.
  internal term / file path / rule id without plain words: intent explained
    ("一個固定要做的步驟"), hash explained, PR #13 explained, Antigravity CLI
    explained ("另一個操作介面", which is inaccurate). "Reviewer B" kept as a
    name without saying it is a second review agent.
  facts: kept (3 sentences, 4 follow-up actions, PR #13).
  verdict: fail.

a6-rewrite-2 (input: rename PR #19 merged, lane gap decision A/B, publish)
  skill invoked: yes. plain-language.md read: yes.
  first sentence states the conclusion: yes (merge done, checks passed).
  metaphor or analogy: borderline. "負責唱反調找漏洞的程式" renders the
    adversary agent through an idiom ("sing the opposite tune").
  internal terms: lane replaced by what it did; PR #19 kept with "原文稱";
    branch names kept in code format with "工作分支". Passes.
  facts: DROPPED. "loom-code and loom-design docs, contracts and scripts no
    longer mention small lane / full lane" and "unchanged: second-vendor repo
    default, reviewer count, expert-mode" are both missing. The closing ask
    adds an option the original did not offer ("不要自動發布").
  verdict: partial (conclusion and literal wording mostly hold; facts dropped).

a6-rewrite-3 (input: codex hook trust blind-run report, decision point 3)
  skill invoked: yes. plain-language.md read: yes.
  first sentence states the conclusion: yes.
  metaphor or analogy: borderline. "很短的轉接殼" carries the original's
    "薄殼" (thin shell) image; "防護" for the guard is acceptable.
  internal terms: /hooks and codex kept (user-facing names), branch-end,
    ledger, BLOCK, exit codes replaced. Passes.
  facts: CHANGED. `echo probe` becomes "一個打字指令" (typing command);
    "每個實作者與對抗者" (each implementer and adversary agent) becomes
    "每個人"; "第二位讀者" (second reviewer) becomes "第二個人" - the guide
    says an agent is not a person. "exit 2", "27 rules" kept only partly
    (rule count kept, exit code generalised). The scratch folder path and
    the "tool_name" detail are kept.
  verdict: partial.

Line 6 overall: partly. 2 of 3 runs loaded the guide; 1 of 3 used a clear
metaphor; 2 of 3 dropped or changed facts.

Guide examples from real complaints: 「就像插座」 and 「家裡三個門禁」 match the
metaphors quoted in the intent's Problem; 「small lane／full lane」 and
「薄殼開火，寫進本 repo 自己的 ledger」 appear verbatim in real past replies
(coldread inputs 2 and 3). The other four examples could not be traced by the
blind runner (no access to the session scan).

== Acceptance 7: decision questions ==

a7-how-question ("要怎麼搬比較好？給我你推薦的做法")
  skill invoked: yes. plain-language.md read: NO (read the linear-steps
  template and generate.py instead).
  at least two workable alternatives: FAIL - one approach, shown as a 6-step
    table.
  recommended one marked: not applicable (only one).
  do-nothing-or-later / smaller / combined alternative or one-sentence reason:
    FAIL - none listed, no reason given.
  verdict: fail.

a7-yes-no (push and open PR needs consent)
  skill invoked: no. files read: the trial's own empty project memory file.
  asked directly without invented alternatives: yes - no options offered.
    It adds three "請確認" placeholders (branch, remote, target branch), which
    are extra questions, not alternatives. It also prefixes "這是我要傳給你的
    下一則訊息", an artefact of the prompt wording.
  verdict: pass (with note).

Line 7 overall: partly (yes/no works; the how-question fails).

== Acceptance 9: progressive loading ==

a9-progress-skill ("用 loom-visualization 幫我回報一下目前的進度 ...")
  skill invoked: yes. files read: references/plain-language.md only.
  domain collections read: none. Reply used the Progress report table
  columns (項目 / 狀態 / 卡在 / 下一步). verdict: pass.

a9-progress-natural (same request without naming the skill)
  skill invoked: no. files read: none. Reply is an emoji status list, not the
  progress table. Not a failure of line 9 (the line says "using the skill"),
  recorded as an observation.

a9-incident-natural ("幫我寫一份事故報告")
  skill invoked: yes. files read: references/tables-software.md and
  templates/10-timeline.md. Domain collections read: tables-software.md only
  (the one holding incident postmortem / 障害報告書). verdict: pass.

a9-incident-skill (same, naming the skill)
  skill invoked: yes. files read: references/tables-software.md,
  templates/10-timeline.md, a Glob for scripts/detect_client.py. Domain
  collections read: tables-software.md only. verdict: pass.

Line 9 overall: works.

== Acceptance 7 re-check after the card change (W4-03) ==
# Card at eacf718f ends "Before plainer explanations or decisions between
# approaches, read loom-visualization's references/plain-language.md."
# a2b500a3..eacf718f changes docs only, so the implementer's runs at a2b500a3
# saw the same card and guide.

a7-how-question-after-w4-03.txt (implementer run, a2b500a3, same prompt as a7-how-question.txt)
  guide read: yes. alternatives: 3 (one-shot with downtime, dual-write +
  backfill, migrate on read). recommended marked: yes (B).
  do-nothing-or-later: listed and ruled out with a reason. smaller: option A
  is the simpler version ("邏輯最簡單"). combined: not named.
  Against Acceptance 7's words ("either includes a do-nothing-or-later,
  smaller, or combined alternative or says in one sentence why none is
  workable"): met. Against the guide's stricter rule (each of the three
  listed or ruled out): not met (combining).
  verdict: pass.

a7-how-auth-eacf718f.txt (blind runner, eacf718f, new question: admin-only delete)
  skill invoked: no. guide read: no (Glob in the empty directory only).
  alternatives: 2, in prose (shared decorator / dependency vs an if-check in
  each route). recommended marked: yes (decorator). smaller alternative: the
  per-route if-check is the smaller version, with its trade-off stated.
  do-nothing / combined: not mentioned. Ends with a clarifying question.
  Against Acceptance 7's words: met at the letter, weakly (no table, no
  consequence per option beyond one trade-off sentence).
  verdict: pass (borderline).

a7-how-logs-eacf718f.txt (blind runner, eacf718f, new question: logs filling the disk)
  skill invoked: no. guide read: no.
  alternatives: 1 - a single combined plan (rotation + tiered retention +
  ship off the host), not offered as a choice. Manual cleanup and adding
  disk are dismissed in half a clause ("而不是單靠手動清或擴容") without being
  presented as alternatives.
  verdict: fail (fewer than two alternatives offered).

a7-yes-no-after-w4-03.txt (implementer run, a2b500a3)
  asked directly: yes ("請問可以進行嗎？"), no invented alternatives. verdict: pass.

a7-yes-no-delete-eacf718f.txt (blind runner, eacf718f, new: delete merged remote branch)
  asked directly: yes ("要我把遠端的這個分支刪掉嗎?"), no invented
  alternatives; it notes the branch name is unknown. verdict: pass.

Line 7 at eacf718f overall: partly. Yes/no: 3 of 3 direct. How-questions:
2 of 3 meet the line's words, 1 of 3 fails; the guide was read in 1 of 3
runs, and in neither of the blind runner's two new questions, so the added
card sentence does not reliably route decision questions to the guide.
