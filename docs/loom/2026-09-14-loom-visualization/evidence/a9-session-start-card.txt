== ascii-graph-toolkit enabled
{"hookSpecificOutput": {"hookEventName": "SessionStart", "additionalContext": "# Visualization trigger card (loom-workflow, ascii-graph-toolkit active)\nBefore you explain any of these in chat, invoke the `loom-visualization`\nskill FIRST, lead with the form it prescribes, then narrate: an option\ncomparison of 2+ options (it picks a markdown table or Mermaid by client),\na branching decision, a reasoning chain, a timeline, quantities to\ncompare, a data model, or a reasoning page. Flows, state machines,\narchitecture, sequences and box-drawing/ASCII diagrams are covered by the\nascii-graph card; follow that card for them. Skip the skill when one\nshort paragraph fully covers the answer. Never draw for decoration.\n"}, "additional_context": "# Visualization trigger card (loom-workflow, ascii-graph-toolkit active)\nBefore you explain any of these in chat, invoke the `loom-visualization`\nskill FIRST, lead with the form it prescribes, then narrate: an option\ncomparison of 2+ options (it picks a markdown table or Mermaid by client),\na branching decision, a reasoning chain, a timeline, quantities to\ncompare, a data model, or a reasoning page. Flows, state machines,\narchitecture, sequences and box-drawing/ASCII diagrams are covered by the\nascii-graph card; follow that card for them. Skip the skill when one\nshort paragraph fully covers the answer. Never draw for decoration.\n", "additionalContext": "# Visualization trigger card (loom-workflow, ascii-graph-toolkit active)\nBefore you explain any of these in chat, invoke the `loom-visualization`\nskill FIRST, lead with the form it prescribes, then narrate: an option\ncomparison of 2+ options (it picks a markdown table or Mermaid by client),\na branching decision, a reasoning chain, a timeline, quantities to\ncompare, a data model, or a reasoning page. Flows, state machines,\narchitecture, sequences and box-drawing/ASCII diagrams are covered by the\nascii-graph card; follow that card for them. Skip the skill when one\nshort paragraph fully covers the answer. Never draw for decoration.\n"}

exit=0
== ascii-graph-toolkit disabled
{"hookSpecificOutput": {"hookEventName": "SessionStart", "additionalContext": "# Visualization trigger card (loom-workflow)\nBefore you explain any of these in chat, invoke the `loom-visualization`\nskill FIRST, lead with the table or diagram it prescribes, then narrate:\nan option comparison of 2+ options, a flow of 3+ steps, a branching\ndecision, a state or lifecycle, an actor sequence, an architecture, or a\nreasoning chain. Before typing any box-drawing diagram (\u250c\u2500\u2510, +--+),\ninvoke `loom-visualization` FIRST. Skip the skill when one short\nparagraph fully covers the answer. Never draw for decoration.\n"}, "additional_context": "# Visualization trigger card (loom-workflow)\nBefore you explain any of these in chat, invoke the `loom-visualization`\nskill FIRST, lead with the table or diagram it prescribes, then narrate:\nan option comparison of 2+ options, a flow of 3+ steps, a branching\ndecision, a state or lifecycle, an actor sequence, an architecture, or a\nreasoning chain. Before typing any box-drawing diagram (\u250c\u2500\u2510, +--+),\ninvoke `loom-visualization` FIRST. Skip the skill when one short\nparagraph fully covers the answer. Never draw for decoration.\n", "additionalContext": "# Visualization trigger card (loom-workflow)\nBefore you explain any of these in chat, invoke the `loom-visualization`\nskill FIRST, lead with the table or diagram it prescribes, then narrate:\nan option comparison of 2+ options, a flow of 3+ steps, a branching\ndecision, a state or lifecycle, an actor sequence, an architecture, or a\nreasoning chain. Before typing any box-drawing diagram (\u250c\u2500\u2510, +--+),\ninvoke `loom-visualization` FIRST. Skip the skill when one short\nparagraph fully covers the answer. Never draw for decoration.\n"}

exit=0
== malformed installed_plugins.json
# Visualization trigger card (loom-workflow)
