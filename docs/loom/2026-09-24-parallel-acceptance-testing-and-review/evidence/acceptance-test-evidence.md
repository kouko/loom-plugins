# Acceptance testing and the first reviewers run on the same version at the same time — acceptance test evidence

Tried on 2026-09-24, in a clean copy of the project at 5263dc7a (control: branch base 710814a9).

## Setup
- How I tried it: `git worktree add --detach $S/wt-head 5263dc7a` and `git worktree add --detach $S/wt-base 710814a9` (`$S` = this session's scratchpad; both `git status --short` empty before and after all trials). The README install path (`claude plugin marketplace add https://github.com/kouko/loom-plugins.git` then `claude plugin install loom-code@loom`, README.md:178-179) installs what is published on main, so the unpublished branch was loaded as a local plugin: `claude -p "$(cat prompt.txt)" --session-id <uuid> --plugin-dir <worktree>/loom-code --output-format stream-json --verbose --dangerously-skip-permissions`, stdout piped through a line timestamper (`$S/pt/ts.py`). No `--disable-slash-commands`. Runner: `$S/pt/run.sh`; timing extractor: `$S/pt/metrics.py`; timeline dumper: `$S/pt/timeline.py`.
- What came back: every session's `init` event listed `loom-code` at the intended worktree, `source: loom-code@inline`, version 3.14.0 (branch) or 3.13.0 (control). Every session's first tool call was `Skill {"skill": "loom-code:closing-review"}`, and the tool result began `Base directory for this skill: $S/wt-head/loom-code/skills/closing-review` (control: `wt-base`). The new sentence "Start the acceptance tester and the first-round reviewers on the same commit at the same time" is in the loaded skill text in all 3 branch sessions and absent in all 3 control sessions. The `init` tool list contains `Task` and `SendMessage`, so `-p` mode can express resuming an agent.
- Evidence: transcripts `$S/pt/{new1,ctl1,new2,ctl2,new3,ctl3}.jsonl`. Claude Code 2.1.281, main model `claude-opus-5-5[1m]`. Total trial cost USD 9.09 (new1 0.50, ctl1 0.59, new2 2.41, ctl2 1.82, new3 2.03, ctl3 1.74).

### The seeded case
- Throwaway repo `$S/pt/seed2`, bare remote `$S/pt/origin2.git` (default branch `main`). `main` (103136f): a line counter `textstat/lines.py` + `test_lines.py`, README, `docs/loom/KICKOFF-DEFAULTS.md` (`package-tests: python3 -m pytest -q`, `docs-lint: none`). Branch `feat/2026-09-24-word-count`: confirmed intent (engineering, `needs-design: no`, 2 Acceptance lines: any whitespace separates words; empty input prints 0) at 560437b, plan at d316f63, change at 630a06d adding `textstat/words.py`, `textstat/cli.py`, `test_words.py`. Deliberate defect: `textstat/words.py:3` uses `text.split(" ")`, so `printf 'one two\nthree\tfour\n' | python3 -m textstat.cli` prints 2, not 4; the tests use spaces only and pass (`4 passed`).
- Dry run of the station's checker calls in a clone: `sync-trunk` → `up to date`; `reviewer-count` → `2`; `selection show` → nothing skipped; `intent` → exit 0.
- Prompt (`$S/pt/prompt.txt`), identical for both arms: run `loom-code:closing-review` on the change; Build's hand-off reports the package suite passing; "I skip the adversarial step for this trial"; stop at the end of Round 1 once the verdicts are in and the fix list is assembled — no Build, no fix, no `finalize-review`, no push; summarize who was dispatched, their verdicts, and the fix list. It says nothing about ordering, parallelism or resuming.
- Each session got its own fresh clone `$S/pt/trial-<name>`.
- First attempt (new1, ctl1) used `$S/pt/origin.git`, whose default branch was the feature branch by mistake (it was cloned bare while the seed had the feature branch checked out). Both sessions stopped at `BLOCK review.sync: HEAD is the trunk branch 'feat/2026-09-24-word-count'` before any dispatch, as the station requires. new1 also noticed the test and CLI files landed in the plan commit. Seed rebuilt as `seed2`/`origin2.git` with clean history (`$S/pt/mkseed.sh`); new1/ctl1 are excluded from all results below.

### Timing table (seconds after the closing-review skill loaded)

| Run | Tester start → return | Reviewer A start → first return → resumed → verdict | Reviewer B start → first return → resumed → verdict | Last Round 1 verdict | Cost |
|---|---|---|---|---|---|
| new2 (branch) | 57.8 → 125.6 | 44.5 → 75.8 → 133.3 → 169.6 | 50.8 → 79.8 → 135.2 → 180.0 | **180.0 (3m00)** | 2.41 |
| ctl2 (trunk) | 27.5 → 210.8 | 225.9 → 252.8 | 233.2 → 281.7 | **281.7 (4m42)** | 1.82 |
| new3 (branch) | 52.4 → 120.4 | 39.7 → 66.0 → 130.7 → 154.8 | 46.0 → 75.8 → 132.0 → 157.9 | **157.9 (2m38)** | 2.03 |
| ctl3 (trunk) | 34.9 → 111.5 | 126.0 → 150.8 | 133.2 → 161.7 | **161.7 (2m42)** | 1.74 |

Source: `task_started` / `task_notification` system events and main-thread `SendMessage` tool calls in each transcript, via `python3 $S/pt/metrics.py <run>.jsonl`.

## 1. In closing review, the acceptance tester and the first-round reviewers start on the same version, and the reviewers begin reading the change without waiting for the acceptance test report.
- How I tried it: branch runs new2 and new3 against control runs ctl2 and ctl3; read every `Agent`/`Task` dispatch (subagent type, `run_in_background`, commit named in the prompt) and the dispatch times. Criterion tests: `python3 -m pytest -q loom-code/scripts/test_parallel_acceptance_review_text.py loom-code/scripts/test_review_convergence_contract.py loom-code/scripts/test_acceptance_test_report_shape.py` in `wt-head`.
- What came back: new2 dispatched reviewer A (44.5 s), reviewer B (50.8 s) and the tester (57.8 s), all backgrounded, all prompts naming commit 97563da (the commit recording the adversarial skip) — the same commit for all three. new3: 39.7 / 46.0 / 52.4 s, all on c03cb18; its status line said "two reviewers plus one acceptance tester go out on the same commit". In both branch runs the reviewers finished a first pass (at 75.8/79.8 s and 66.0/75.8 s) while the tester was still running. Control: ctl2 and ctl3 dispatched the tester alone in the foreground ("Next I'm dispatching the independent acceptance tester. The reviewers go after it."), and the reviewers only after the report commit, with prompts naming the report commit (02e485f, d0d06be). Tests: `test_parallel_acceptance_review_text.py` 3 passed (`test_new_rules_are_pinned_exactly`, `test_weakened_or_appended_rewrites_fail`, `test_one_fix_list_report_shape_gates_and_rules_kept`); with the other two files, 50 of the 50 tests in those three files passed (recounted at 5263dc7a in `wt-head`: `50 passed`; an earlier draft said 61, which was wrong).
- Evidence: `$S/pt/new2.jsonl`, `new3.jsonl`, `ctl2.jsonl`, `ctl3.jsonl`; rule text `loom-code/skills/closing-review/SKILL.md:115-117,209-223`. The full suite (the declared `package-tests` command in `docs/loom/KICKOFF-DEFAULTS.md`, run by `finalize-review`) was not run here, per the tester contract.

## 2. The same reviewers read the committed acceptance test report and its evidence file before giving their verdict, in the same round, so the report is still reviewed and committing it causes no extra review round.
- How I tried it: in new2 and new3, matched each `SendMessage` result's `resumedAgentId` against the agent id returned at dispatch; read the resume message, each reviewer's first and second return, and the final summary.
- What came back: new2 `SendMessage {"to": "reviewer-a"}` → `{"success":true,"message":"Resuming agent reviewer-a","resumedAgentId":"a978acd3faf80e41c"}`, the same id as the reviewer-A dispatch; same for reviewer-b (`aab37d433ab4a16b9`). new3 likewise. Resume messages named the delta: new2 "Delta 97563da..e718002 adds only docs/loom/2026-09-24-word-count/acceptance-test-report.md and …/evidence/acceptance-test-evidence.md"; new3 "Delta c03cb18..eaa810a …". The resumes were sent 7.7 s (new2) and 10.3 s (new3) after the tester's return, i.e. after its report commit. First returns were labelled not a verdict: new2 A "**Preliminary findings, not my verdict.**", new2 B "This is not my verdict. I will give the verdict after I am resumed"; new3 A "provisional review, not the Round 1 verdict", new3 B "Provisional Round 1 review (code lens): NEEDS_REVISION. This is not my final verdict." The orchestrator treated them as such in both runs (new2: "That isn't a verdict yet."). Second returns read the report and evidence: new2 A "The acceptance-test report states this correctly. It doesn't overclaim and it tries every separator the Acceptance line names", citing `acceptance-test-evidence.md:23-27`; new2 B re-ran the evidence rows and checked the report's claim that finalize-review would not run the suite. Final summaries: new2 "Round 1 covers the change from `103136f` to `e718002`"; new3 "Round 1 covers the content up to `eaa810a`". Neither run opened a second round for the report.
- Caveat: in new3 both provisional returns already carried a YAML `verdict: NEEDS_REVISION` line (found by searching each return for `verdict:`). The orchestrator ignored them, but the recorded label and the provisional one are indistinguishable by field.
- Caveat (outside this change): in new3 the orchestrator told the tester "finalize-review is NOT skipped" although the user skipped adversarial in plain words, which §5 says skips finalize-review. The tester's row 2 then said the suite runs before acceptance, and neither reviewer flagged it. In new2 the orchestrator said finalize-review IS skipped, and both reviewers checked that claim. The same §5 text is on trunk.
- Evidence: transcripts above; reviewer contract `loom-code/agents/reviewer.md:97-106`.

## 3. Findings from acceptance testing and from the reviewers on that version reach the fix round as one list.
- How I tried it: read the final "fix list" in each run and each source's findings.
- What came back: new2 "Fix list for Build (one hand-off: tabs and line breaks aren't treated as word separators)": fatal `textstat/words.py:3` + important `test_words.py:4-5`, merging the tester's findings (fatal, important, and the nit about newline-only input printing 1, folded into item 1) with both reviewers' matching findings; nits listed separately. new3: same one hand-off with both items and the whitespace-only case from the tester. All three sources ran on the same content, which ended at the report commit. Control ctl2/ctl3 also produced one list and one hand-off.
- Caveat: every source raised the same defect, so this seed cannot show a tester-only finding being merged with a reviewer-only one, and it does not tell the arms apart on this line.
- Evidence: final `result` text in `new2.jsonl`, `new3.jsonl`; `test_one_fix_list_report_shape_gates_and_rules_kept` passed.

## 4. The acceptance test report the user reads at acceptance still states, one row per Acceptance line, what was tried and the result.
- How I tried it: `git diff --stat 710814a9..HEAD -- loom-code/skills/closing-review/references/ loom-code/agents/acceptance-tester.md`; `python3 -m pytest -q loom-code/scripts/test_acceptance_test_report_shape.py`; read the reports committed in the trial clones.
- What came back: empty diff (exit 0), so the report template and the tester contract are unchanged; `16 passed`. `trial-new2` and `trial-new3` reports each have exactly one row per Acceptance line (row 1 `fails`, row 2 `works`) with a plain sentence.
- Evidence: commands above; `$S/pt/trial-new2/docs/loom/2026-09-24-word-count/acceptance-test-report.md`, `$S/pt/trial-new3/…`. The full package suite is executed by `finalize-review`, which runs before this change is accepted and refuses the attestation on failure.

## 5. Given a trial change run through closing review with both steps, the time from the start of closing review to the reviewers' verdicts is shorter than running acceptance testing and then the reviewers one after the other, shown by a trial run.
- How I tried it: two paired runs (new2/ctl2 in parallel, then new3/ctl3 in parallel) on the same seed and prompt; timing table above.
- What came back: pair 1: branch 180.0 s vs control 281.7 s (−101.7 s). Pair 2: branch 157.9 s vs control 161.7 s (−3.8 s). Pair 1's gap is mostly not this change: the ctl2 tester spent 2m02 on `find / -path "*loom-code/skills/closing-review/references/acceptance-test-report.md"` (started 41.8 s, stopped 2m44 s), because its prompt did not give the template path, which both branch prompts did. Its tester ran 183 s against 68 s in new2. Pair 2 had comparable testers (68 s vs 77 s). The branch hid the reviewers' first pass (26–30 s) behind the tester. But the resumed read of the report took 24–45 s, about as long as a whole trunk review of this three-file change (25–48 s). The branch also dispatched later (first dispatch 39.7–44.5 s vs 27.5–34.9 s to the tester). Net: shorter in both pairs, but the one clean pair differs by 4 s, which is within run-to-run noise.
- Cost: branch runs cost USD 0.29–0.59 more than their control pair (2.41 vs 1.82; 2.03 vs 1.74), from the extra resume turns.
- Evidence: timing table; `ctl2.jsonl` subagent Bash calls `Read kickoff defaults and locate report template` (the `find /`) and `Read kickoff defaults and find template dir` (which then looked in `~/GitHub/loom-plugins/loom-code/skills/closing-review/references`, outside the trial's plugin worktree).

## 6. The checker's rule list does not grow and no new step, reviewer or dispatch is added.
- How I tried it: `python3 loom-code/scripts/loom_checker.py --list-rules | wc -l` in both worktrees; `python3 loom-code/scripts/check_mechanisms.py --baseline 710814a9` in `wt-head`; `git diff 710814a9..HEAD -- loom-code/skills loom-code/agents | grep '^+' | grep -iE "gate:|dispatch|subagent|new step"`; `python3 -m pytest -q loom-code/scripts/test_write_plan_station_text.py -k synchronized`; counted agent dispatches per trial.
- What came back: 26 rules on base and branch; `prose-gate 20 20`, `net mechanism count (excl. host-hygiene): 140`, `baseline net count: 140`, `all clear`; the grep matched no added line; `1 passed, 48 deselected`. Every trial dispatched exactly 3 agents (1 tester + 2 reviewers), matching `reviewer-count` = 2. The branch adds 2 `SendMessage` resumes of existing agents, not new dispatches.
- Evidence: commands and outputs above.

## Other observation
- `python3 -m pytest -q loom-code/skills/closing-review/probes/test_recovery_rules.py` → `1 failed, 11 passed` on both `wt-head` and `wt-base` (`RL-06 FAIL: paragraph 'Stop and ask when producing an absent item needs' is missing ['expert-mode']`). This failure was already on trunk. The branch edits this file (line 62) and leaves it failing. `scripts/run_package_tests.py` does not collect `loom-code/skills/*/probes/`, so the package suite does not run it.

## Re-run on 2026-09-24, at ee26369c
Fix range 9c0cbb98..ee26369c: `loom-code/skills/closing-review/SKILL.md` (tell each first-round reviewer whether acceptance testing runs alongside; a re-test after a fix commits its report and evidence before the reviewers are resumed), `loom-code/agents/reviewer.md` (optional `acceptance testing: alongside` input line; interim first return with `status: interim` and no `verdict:`; report findings filed under the lens's closest dimension; Fix-rounds heading names Round 2/3), `test_parallel_acceptance_review_text.py`, `CHANGELOG.md`.

- Setup: `git worktree add --detach $S/wt-fix ee26369c` (`git status --short` empty); plugin versions 3.14.0 in both manifests. One branch trial `bash $S/pt/run.sh fix1 $S/wt-fix` on the same seed (`origin2.git`, fresh clone `$S/pt/trial-fix1`) and the same `prompt.txt`; transcript `$S/pt/fix1.jsonl`, agent returns dumped to `$S/pt/fix1-returns.txt` (extractor `$S/pt/fixcheck.py`, timings `$S/pt/metrics.py`). `init` listed `loom-code` at `$S/wt-fix/loom-code`, `source: loom-code@inline`, version 3.14.0; first tool call `Skill loom-code:closing-review`. Claude Code 2.1.281. Exit 0; cost USD 2.05. No control arm: trunk 710814a9 is unchanged, so ctl2/ctl3 above still stand as the control.
- Criterion tests at ee26369c: the three files from row 1 → `51 passed` (the fix adds `test_reviewer_is_told_it_runs_alongside_and_returns_interim_first`; `test_parallel_acceptance_review_text.py` → 4 passed).
- Timing (s after skill load): tester 46.1 → 104.3; reviewer A 33.9 → 57.3 (interim) → 111.2 resumed → 130.5; reviewer B 39.9 → 64.5 (interim) → 112.3 resumed → 136.4. Last Round 1 verdict **136.4 (2m16)**.

- 1: re-tested — reviewer A (33.9 s), reviewer B (39.9 s) and the tester (46.1 s) all backgrounded, all prompts naming start commit 7ee719e (the adversarial-skip commit). Both reviewer prompts carried "Independent acceptance testing IS running alongside this round. Its report will land at docs/loom/2026-09-24-word-count/acceptance-test-report.md. Do your review now and return your preliminary find…" — a prose paraphrase, not the literal `acceptance testing: alongside` input line (SKILL.md §2 does not require the literal line; the reviewers behaved as the line prescribes). Both reviewers returned a first pass (57.3, 64.5 s) before the tester returned (104.3 s). Criterion tests: 51 passed (above). The full suite (`package-tests` in `docs/loom/KICKOFF-DEFAULTS.md`) is left to `finalize-review`.
- 2: re-tested — `SendMessage` to reviewer-a → `resumedAgentId":"a084e87a44527ca32"` (= dispatch agentId), reviewer-b → `adfa78ab87740049e` (= dispatch agentId). Resumes at 111.2/112.3 s, after the tester's report commit 51fe097 ("docs(loom): acceptance test report for 2026-09-24-word-count"); message: "Delta from your start commit: 7ee719e..51fe097 (adds …acceptance-test-report.md and …/evidence/acceptance-test-evidence.md)". Interim returns: both YAML blocks carry `status: interim`, and neither return contains the string `verdict:` (searched each return); prose "The interim return is below. It has no verdict yet" (A), "Preliminary (interim) result" (B). This closes the earlier caveat (new3's provisional returns carried `verdict: NEEDS_REVISION`). Final returns carry `verdict: NEEDS_REVISION`, `reviewed_sha: 51fe097`; A: "I re-derived the report's numbers … the 9-word README sample splits into 7 fields"; B: "I re-ran three of its cases at 51fe097" and filed a report finding (`acceptance-test-report.md:22`, open question already settled by Acceptance 1) as `severity: nit, dimension: correctness` — the lens's closest dimension, per the new rule. Orchestrator: "Each reviewer first sent back findings without a verdict. After the acceptance report was committed, I resumed both of them … they gave their final verdicts then." No second round opened. Not exercised: the new §3 ordering for a re-test after a fix (the trial stops at Round 1); pinned only by `test_new_rules_are_pinned_exactly` / `test_weakened_or_appended_rewrites_fail`. Small observation: reviewer A's interim `reviewed_sha` was 103136f (branch base), B's 7ee719e; both finals 51fe097.
- 3: re-tested — final result: "Fix list for Build (one hand-off, because the findings share one defect …)": fatal `textstat/words.py:3` ("Both reviewers and the acceptance tester found this", including the tester's whitespace-only-prints-1 case) + important `test_words.py:4`; nits listed separately, including reviewer B's report nit. Caveat unchanged: every source found the same defect, so a tester-only finding merged with a reviewer-only one is still not shown.
- 4: carried over — `git diff --stat 710814a9..ee26369c -- loom-code/skills/closing-review/references/ loom-code/agents/acceptance-tester.md` is empty and the fix diff touches neither; `test_acceptance_test_report_shape.py` → 16 passed at ee26369c.
- 5: re-tested — fix1 136.4 s vs ctl3 161.7 s (−25.3 s) and ctl2 281.7 s. Not a same-time pair; fix1's tester ran 58.2 s vs ctl3's 76.6 s, which accounts for ~18 s of the gap. The overlap again hid the reviewers' first pass (23–25 s) behind the tester; the resumed read took 19–24 s. Net: shorter, but within run-to-run and tester-speed noise on this small change. Branch cost 2.05 vs ctl3 1.74.
- 6: re-tested — at ee26369c: `loom_checker.py --list-rules | wc -l` → 26; `check_mechanisms.py --baseline 710814a9` → `prose-gate 20 20`, `net mechanism count (excl. host-hygiene): 140`, `baseline net count: 140`, `all clear` (140 is the net mechanism count, not a gate count; the prose-gate count is 20); `git diff 710814a9..HEAD -- loom-code/skills loom-code/agents | grep '^+' | grep -iE "gate:|dispatch|subagent|new step"` → no match (exit 1); `test_write_plan_station_text.py -k synchronized` → 1 passed, 48 deselected. fix1 dispatched exactly 3 agents (2 `loom-code:reviewer`, 1 `loom-code:acceptance-tester`) plus 2 `SendMessage` resumes.
