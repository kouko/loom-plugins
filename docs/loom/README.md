# docs/loom/ — the loom store

Everything a loom change produces lives here. Since loom 1.0
(2026-09-03) the layout is fixed and small: a change is born as an
intent, grows a folder, and leaves evidence behind. Nothing else in
this directory is written by the stations.

## Live layout

| Entry | What it holds | Written by |
|---|---|---|
| [`intent/`](intent/) | One `<change-id>.md` per change — the entry artifact for every change, live or closed. Problem, proposed outcome, acceptance, constraints, open questions; its status line drives what the stations will accept | capture-intent (loom-design) or write-plan (loom-code) |
| `<change-id>/` | The change folder: `spec.md` (only when the change needs design), `plan.md` (the Task DAG), `blind-run-report.md` (when a blind run ran), `attestation.json` (generated by `finalize-review`, bound to the functional-content digest), `evidence/` (everything a reviewer, blind runner or end-of-Build adversary produced for THIS change) | write-spec, write-plan, build, closing-review |
| [`evidence/`](evidence/) | Repo-level evidence — the records that outlive one change: [`mechanisms.yaml`](evidence/mechanisms.yaml) (the mechanism inventory the recompute gate diffs against) and the inherited store `outcome-map-v3/` | closing-review; anyone recording a measurement |
| [`memory/`](memory/) | Practice-memory store — one distilled fact per file, the knowledge that must travel with the repo. Charter in [`memory/README.md`](memory/README.md) | closing-review at convergence, in a digest the reviewers read |
| [`maps/`](maps/) | Persistent decision maps — `MAP.md` plus tickets per run. A map that wants a slice delivered writes an intent carrying `map:`; it never owns a delivery ticket of its own | [`loom-workflow:decision-map`](../../loom-workflow/skills/decision-map/) |
| [`KICKOFF-DEFAULTS.md`](KICKOFF-DEFAULTS.md) | This repo's standing answers to the questions the stations would otherwise ask every time — second vendor, package-test command, standing-docs waiver, interface surfaces, session-start baseline. One line per key, read by `loom_checker.py` | a human, once per repo |

The artifact type of any path is decided by the mapping in the change's
concept model §6 — `docs/loom/intent/**` is an intent, `**/evidence/**`
is evidence, `docs/loom/memory/**` is memory, `docs/loom/maps/**` is a
map. That mapping is what tells Build and the review station which of
the three verification actions a file owes: the adversarial programs at
the end of Build, then read and blind run in review.

## Frozen stores — read-only history

These are the pre-1.0 stores. loom 1.0 was a hard cutover: old plans,
specs, briefs and backlog entries were archived where they stood and
never converted. They stay in
the tree because `git log --grep` and `grep -rn` against historical
decision context is why they were kept in the first place — but no
station reads them, and nothing new should be written into them.

- [`plans/`](plans/) — writing-plans output, 2026-05-18 → 2026-09-01

## checkpoint 的三個驗證角色什麼時候被觸發

盲跑者＝`blind-runner`、讀者＝`reviewer`、對抗者＝`adversary`
（另有實作者＝`implementer`）。圖中縮寫：impl／adv／blind／rev x2。
對抗者在 end of Build 出場，盲跑者與讀者在收尾審查出場。
步驟整體先後排列；僅 5 的兩位讀者彼此並行。

```
┌───────┐   ┌──────┐   ┌─────┐   ┌────────┐   ┌───────┐   ┌────────┐   ┌──────┐
│ build │   │ impl │   │ adv │   │ review │   │ blind │   │ rev x2 │   │ ship │
└───┬───┘   └───┬──┘   └──┬──┘   └────┬───┘   └───┬───┘   └────┬───┘   └───┬──┘
    │           │         │           │           │            │           │   
    │           │         │           │           │            │           │   
    │  1 實作   │         │           │           │            │           │   
    │───────────►                                                              
    │     2 對抗程式      │           │           │            │           │   
    │─────────────────────►                                                    
    │           │ 3 交接  │           │           │            │           │   
    │─────────────────────────────────►                                        
    │           │         │           │  4 盲跑   │            │           │   
                                      │───────────►                            
    │           │         │           │       5 並行讀         │           │   
                                      │────────────────────────►               
    │           │         │           │       6 verdict        │           │   
                                      ◄────────────────────────│               
    │           │ 7 修正  │           │           │            │           │   
    ◄─────────────────────────────────│                                        
    │           │         │           │           8 attestation│           │   
                                      │────────────────────────────────────►   
```

```json
{
  "participants": [
    "build",
    "impl",
    "adv",
    "review",
    "blind",
    "rev x2",
    "ship"
  ],
  "messages": [
    {
      "from": "build",
      "to": "impl",
      "label": "1 實作"
    },
    {
      "from": "build",
      "to": "adv",
      "label": "2 對抗程式"
    },
    {
      "from": "build",
      "to": "review",
      "label": "3 交接"
    },
    {
      "from": "review",
      "to": "blind",
      "label": "4 盲跑"
    },
    {
      "from": "review",
      "to": "rev x2",
      "label": "5 並行讀"
    },
    {
      "from": "rev x2",
      "to": "review",
      "label": "6 verdict"
    },
    {
      "from": "review",
      "to": "build",
      "label": "7 修正"
    },
    {
      "from": "review",
      "to": "ship",
      "label": "8 attestation"
    }
  ]
}
```

重生：JSON 存成 payload.json，`ascii-graph` 的 seq 生成器：
`python3 generate.py seq < payload.json`。

| 步 | 誰派誰 | 內容 | 同步性 |
|---|---|---|---|
| 1 | build→impl | TDD、一 task 一 commit | 同波可並行 |
| 2 | build→adv | 所有 task 落地、sync-trunk 後寫並 commit 對抗程式 | end of Build；接著跑完整套件與每支程式 |
| 3 | build→review | 套件與對抗程式全綠才交接 | 分支尾，涵蓋整條分支 |
| 4 | review→blind | 逐條走 Acceptance、commit 報告 | 讀者讀之前落地 |
| 5 | review→rev x2 | 兩位讀者判讀 | 等盲跑報告落地才派、互不可見 |
| 6 | rev x2→review | verdict 綁功能內容 digest | 不平均，一票 NEEDS_REVISION 定案 |
| 7 | review→build | 修正回到 Build，重跑結尾檢查 | 同一 episode 最多三個 digest |
| 8 | review→ship | `finalize-review` 重跑套件與對抗程式、生成 attestation | 之後 ship 才發布 |

盲跑報告要在讀者讀之前 commit，因為 verdict 綁 digest——晚進樹的
報告是新的功能內容，要下一輪才讀得到。`selection show` 列為 skipped
的步驟直接省略。

三方歸屬：一條 finding 不是「我的」就一定是別人的，判給誰要照
它本來就屬於誰，不是誰先讀到。讀者收：遺漏、誇大、矛盾、跨文件
計數——凡是要對照 spec／intent／既有文件才看得出的落差。對抗者
收：探針自己的產物、邊界與敵意輸入、被遺忘的狀態——凡是要主動
戳一下系統才冒出來的問題。實作者收：正向的、可執行的 RED——缺的
happy-path 測試，不是別人挑出的毛病。對抗者契約在 N=10 冷讀下
三方歸屬 80/80（字數帽改前）與 79/80（改後），沒有任何一條在
≥50% 的跑次錯，所以這句三方歸屬的措辭不再改（量測方法與逐條分佈
見
`docs/loom/2026-09-04-adversary-three-way-attribution-measured/`
下的 `evidence/baselines.md`）。
讀者契約作為對照組是 52/80→66/80，改後仍把第 2、6 條（邊界類）
當成自己的（6/10、8/10）——這屬於讀者契約的問題，不在本 change
範圍，先記待辦。

## Where a new change starts

Write `docs/loom/intent/<date>-<slug>.md` (template:
`loom-code/contract/templates/intent.md`), confirm it with the user,
and hand it to the write-plan station. Everything else — the change
folder, the spec if one is needed, the plan, the attestation — is created
by the stations from there.
